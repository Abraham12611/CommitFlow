// Import the generateCommitMessage function from the appropriate module
import { generateCommitMessage } from '../lib/generateMessage.js';

// If there are additional utility functions or setup, they can be added here.
// For example, this can be extended later if there are multiple exportable features.

// Export the generateCommitMessage function to be used in other files
export { generateCommitMessage };
